#ifndef HEADER_FILE_H   //If HEADER_FILE_H isn't defined, to enable definition to #endif
#define HEADER_FILE_H   //Define HEADER_FILE_H

void mySwap(int& a, int& b){};
void mySwap(double& a, double& b){};

#endif